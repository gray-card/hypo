// Compatibility facade. The strict, side-effect-free implementation lives in @hypo/domain.
export { CELL_DEG, buildPhotoIndex, coarseCell, emptyFilterState, filterIsEmpty, photoMatches } from "@hypo/domain";
