name: Release Hypo

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate-tag:
    name: Validate release tag
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      commit: ${{ steps.release.outputs.commit }}
      version: ${{ steps.release.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch main
        run: git fetch --no-tags origin main:refs/remotes/origin/main
      - id: release
        name: Verify the tag, package version, and main ancestry
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([+-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Release tags must use semantic versioning (for example, v1.0.0)." >&2
            exit 1
          fi

          PACKAGE_VERSION="$(node -p 'require("./package.json").version')"
          if [[ "$RELEASE_TAG" != "v$PACKAGE_VERSION" ]]; then
            echo "Tag $RELEASE_TAG does not match package version $PACKAGE_VERSION." >&2
            exit 1
          fi

          RELEASE_COMMIT="$(git rev-list -n 1 "$RELEASE_TAG")"
          MAIN_COMMIT="$(git rev-parse origin/main)"
          if [[ "$RELEASE_COMMIT" != "$MAIN_COMMIT" ]]; then
            echo "Tagged commit $RELEASE_COMMIT must be the current main commit $MAIN_COMMIT." >&2
            exit 1
          fi

          echo "commit=$RELEASE_COMMIT" >> "$GITHUB_OUTPUT"
          echo "version=$PACKAGE_VERSION" >> "$GITHUB_OUTPUT"

  gates:
    name: Run release gates
    needs: validate-tag
    permissions:
      contents: read
    uses: ./.github/workflows/ci.yml

  github-release:
    name: Publish GitHub Release
    needs:
      - validate-tag
      - gates
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Create or repair the GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
          RELEASE_VERSION: ${{ needs.validate-tag.outputs.version }}
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release edit "$RELEASE_TAG" --title "Hypo $RELEASE_VERSION"
          else
            gh release create "$RELEASE_TAG" --verify-tag --generate-notes --title "Hypo $RELEASE_VERSION"
          fi

          echo "### Hypo $RELEASE_VERSION released" >> "$GITHUB_STEP_SUMMARY"
          echo "The current main commit passed all release gates and has a GitHub Release. GitHub Pages deployment remains restricted to main CI." >> "$GITHUB_STEP_SUMMARY"
