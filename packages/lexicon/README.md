# `@hypo/lexicon`

Generated TypeScript record types, namespace constants, known-value hints, and
runtime validators for every schema under `../../lexicons/`.

Run `npm run generate:lexicons` after changing a lexicon. Generated output is
committed so schema drift is visible in review and can be checked in CI.
